import React, { Component } from 'react';

import { fetchActorInfos } from './../services/fetchMoviesDatabase';

export class ActorPage extends Component {
  render() {
    console.log(this.props);
    return <div>ACTOR PAGE</div>;
  }
}
